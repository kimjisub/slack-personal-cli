# awesome-* list entries

Objective, one line. Targets: `awesome-cli-apps`, `awesome-slack`. Check each
list's contribution rules (alphabetical order, license tag format) before the PR.

## List entry (one line)

```
- [slack-personal-cli](https://github.com/kimjisub/slack-personal-cli) - Zero-setup macOS Slack CLI that works across all your signed-in workspaces at once (unread digest, unanswered-mentions, cross-workspace search); session-based, no OAuth app or bot token. `MIT`
```

## PR description

```
Adds slack-personal-cli — a macOS Slack CLI that reuses the desktop app's
session (no OAuth app / bot token) and uniquely operates across all signed-in
workspaces at once. Zero runtime dependencies, MIT-licensed, has tests + CI.
```
